import React from 'react';
import LandingPage from '../features/landing/LandingPage';

const Home: React.FC = () => {
    return (
        <main>
            <LandingPage />
        </main>
    );
};

export default Home;
